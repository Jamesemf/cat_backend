# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Database (SQLite for dev, Postgres for prod — driver selected by DATABASE_URL scheme).
# geoalchemy2 goes in later if/when we adopt PostGIS for spatial indexing.
sqlalchemy==2.0.40
alembic==1.15.2
psycopg2-binary==2.9.10

# Data validation (pinned alongside FastAPI)
pydantic==2.11.4
pydantic-settings==2.9.1

# File uploads & image processing
python-multipart==0.0.20
Pillow==11.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt >= 4.1 (its backend-probe hashes a
# 72-byte string, which newer bcrypt rejects with a ValueError). Pin to 4.0.1.
bcrypt==4.0.1

# Object storage (photos)
boto3==1.38.9

# HTTP client (ML API calls)
httpx==0.28.1

# Claude Haiku vision recognition
anthropic==0.40.0

# Environment variables
python-dotenv==1.1.0

# Testing
pytest==8.3.5
